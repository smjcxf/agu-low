# 阿狸咪 → 小九 交接
## 2026-06-17（周三）早上

---

## 阿狸咪端要做的事（就一条命令）

```bash
cd E:/workspace/stock-scanner && git pull origin main
```

拉完自动同步以下所有修改。

---

## 今天代码变更汇总（已全部推到 main 分支）

| 提交 | 内容 |
|------|------|
| `5bb509e` | 四喜临门为0时自动隐藏，有信号才出现 |
| `3c50f71` | 三线共振统一取金股池——所有页面永久一致 |
| `e8fb8bb` | 改密码（管理员 cat999 / 贵宾 hjd666）+ pwd.yaml 进 gitignore |
| `da6ff78` | 移除敏感 zsxq_token.json |
| `c62b853` | 金股池保留天数 20→45个交易日（约两个自然月） |
| `f42c142` | 三线共振历史保护：空数组不覆盖旧数据 |

---

## 定时任务变更（阿狸咪侧也照改）

**19:30 收盘全量**拆成两个任务：

| 时间 | 任务 | 命令 |
|------|------|------|
| 19:31 | 收盘扫描+采集 | 4组并行采集 + `scanner.py full` → `generate_recommend.py` → `update_data_v2.py`（不部署） |
| 19:36 | 收盘部署 | `deploy_now.py`（单独部署） |

拆开是为了防止超时。

---

## 注意事项

- 逻辑详解页不动
- 知识星球 Token 已更新本地，git pull 不影响（在 gitignore 里）
- 以后两边数据自动同步：deploy 前会从 git 拉对方更新的 data/ 文件

— 小九
2026-06-17 08:43

# 交接文档 — 2026年6月17日 @阿狸咪

## 今天做了 12 项修改，全部已部署上线

---

### 一、数据修复（5项）

**1. 资金抱团数据3天未更新**
- 根因：NeoData token 过期 + 子进程 GBK 编码问题
- 修复：`fetch_herding_data.py` 编码改为 `encoding='utf-8'`，token 通过 `connect_cloud_service` 刷新
- token 有效期 12 小时，过期后需手动刷新
- 另修复：`extract_sector_flows()` 列索引修正（板块名称 parts[4]→[5]）、broker_views 去重

**2. 过期催化剂/谨慎方向自动过滤**
- `fetch_herding_data.py` 新增日期解析和过滤逻辑
- 催化剂："6.12-14"类已过期的自动移除，"6.18"未来的保留
- 谨慎方向：含"大会临近尾声/已充分定价"等关键词的自动移除

**3. 北向资金时间戳造假修复**
- `fetch_north_fund.py`：API无数据回退旧数据时，不再篡改 `update_time` 为当前时间
- 旧数据保留原始时间戳（如 6/14），前端能正确识别时效

**4. 三线共振历史数据不一致修复**
- `generate_triple_resonance_history.py`：当日数据不再从 GOLD_POOL 历史读取（残留假数据）
- 改为仅从 SCAN_DATA + WATCH_DATA 实时数据源，与首页一致

**5. ETF资金流向未及时更新**
- 在09:15盘前和10:00盘中任务中加入了 `fetch_etf_flow.py`

---

### 二、前端功能（5项）

**6. Tab顺序调整**
- 预判信号移到数据监控后面：总览 → 数据监控 → 🔮预判信号 → 金股观测 → 个股查询 → 逻辑详解

**7. T+1数据备注**
- 两融余额走势、宽基ETF净申赎标题旁加"（T+1日更新）"，金色标注

**8. 盘后数据免告警**
- 龙虎榜、主力资金、北向资金均为盘后T+1数据
- 移除"API暂无新数据"/"非交易日暂无数据"提示
- 旧数据正常显示但不标过期警告，周末展示周五数据

**9. 催化剂保留状态标注**
- 每个催化剂事件后面显示："明天"/"持续中"/"即将"/"今日" 等状态

**10. 拼音首字母搜索**
- 个股查询支持拼音首字母输入：如输"hlzj"搜"海澜之家"

---

### 三、策略调整（1项）

**11. 新增"趋势确认"信号组合**
- 机构变红 + 上涨趋势 = 趋势确认（中长线趋势型）
- 之前漏掉了生益科技(+8.28%)、蓝思科技(+7.29%)、TCL中环(+2.23%)等中长线趋势股
- 不同组合对应不同策略：短线突破（缠论+金钻） vs 中长线趋势（机构+趋势）

当前四组信号排序：
| 优先级 | 标签 | 信号 | 评分 |
|--------|------|------|------|
| 1 | 🏮四喜临门 | 四个全满足 | 113 |
| 2 | ⚡三线共振 | 任意3个 | 75~88 |
| 3 | 📈缠论+金钻 | 短线突破型 | 55~60 |
| 4 | 💎趋势确认 | 机构+趋势，中长线 | 53 |

---

### 四、模板持久化（1项）

**12. update_data_v2.py 模板路径修正**
- 从 `dist/index_master.html` 改为根目录 `index_master.html`
- 编辑根模板即可，定时任务自动生效，无需手动同步到 dist/

---

## ⚠️ 需要阿狸咪同步的

### 定时任务（自动化）
阿狸咪那边的定时任务需要同步以下改动：

**19:31 收盘扫描任务** — 添加了一步：
```
scanner.py full → generate_triple_resonance_history.py → generate_recommend.py → update_data_v2.py
```

**09:15 盘前任务** — 加入并行：
```
guanlan_extractor.py + fetch_etf_flow.py → scanner.py a → ... → deploy
```

**10:00 盘中扫描** — 加入：
```
fetch_etf_flow.py → scanner.py quick → update → deploy
```

### 代码文件
以下文件已修改，需要 git pull 同步：
- `fetch_herding_data.py`（编码+列索引+过滤+去重）
- `fetch_north_fund.py`（时间戳修复）
- `generate_triple_resonance_history.py`（当天数据源修复）
- `update_data_v2.py`（模板路径）
- `index_master.html`（所有前端改动）
- `triple_resonance.html`（历史追踪页）
- `cleanup_cf.py`（新增 Cloudflare Pages 清理脚本）

---

## 当前网站密码
- 管理员：`cat999`
- 贵宾：`hjd666`
